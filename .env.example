# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=afripay
DB_PASSWORD=devpass123
DB_NAME=afripay

# Application Configuration
PORT=3000
NODE_ENV=development

# For production, use AWS Secrets Manager instead
# DB_PASSWORD should NEVER be hardcoded in production
# ============================================
# Database Configuration
# ============================================
DB_USER=afripay
DB_PASSWORD=change_me_in_production
DB_NAME=afripay

# ============================================
# Port Configuration
# ============================================
# PostgreSQL port (host machine)
POSTGRES_PORT=5432

# Application port (host machine)
APP_PORT=3000

# Nginx port (host machine)
NGINX_PORT=8080

# Internal ports (don't change unless needed)
POSTGRES_INTERNAL_PORT=5432
APP_INTERNAL_PORT=3000
NGINX_INTERNAL_PORT=80

# ============================================
# Application Settings
# ============================================
NODE_ENV=development
LOG_LEVEL=info

# ============================================
# Database Connection (for app)
# ============================================
DB_HOST=postgres
DB_PORT=5432

# ============================================
# Health Check Settings
# ============================================
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=3s
HEALTH_CHECK_RETRIES=3

# ============================================
# Instructions:
# 1. Copy this file to .env
# 2. Change values as needed for your environment
# 3. Never commit .env to git!
# ============================================
